import React from 'react';
import Carousel from '../Carousel'
import * as rtl from '@testing-library/react'

describe('Carousel', () => {
  it('works', () => {
    rtl.render(
      <Carousel>

      </Carousel>
    )
  })
})
